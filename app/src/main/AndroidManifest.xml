<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.SMDAssignment4"
        tools:targetApi="31">

        <!-- Splash Screen Activity -->
        <activity android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Activity -->
        <activity android:name=".LoginActivity" />
        <activity android:name=".ShoppingListActivity" />
        <activity android:name=".AddItemActivity" />
        <!-- Other activities like RegisterActivity, ShoppingListActivity should also be declared here -->
    </application>

    <!-- Add permission for internet access if not already included -->
    <uses-permission android:name="android.permission.INTERNET" />

</manifest>
